<%= link_to bookmark_path(current_user.bookmarks.find_by(pose_id: pose.id)), id: "unbookmark-button-for-pose-#{pose.id}", data: { turbo_method: :delete } do %>
  <i class="bi bi-star-fill"></i>
<% end %>